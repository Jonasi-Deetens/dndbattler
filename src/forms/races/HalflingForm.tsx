import React from "react";

const HalflingForm: React.FC = () => {
  return (
    <div>
      <h2 className="border p-2">Halfling</h2>

      <p>No options to select.</p>
    </div>
  );
};

export default HalflingForm;
